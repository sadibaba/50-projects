Aye aye! 🔥 **End-to-End API Flow** — Try/Catch ke saath **50+ real world APIs** ka complete ecosystem bana raha hoon. Har API mein proper flow hoga:

**Request → Try/Catch → Response Handling → Error Management → Data Transformation**

---

## 🌐 PART 1: USER AUTH ECOSYSTEM (10 APIs)

### 1.1 Register
```js
const registerUser = async (userData) => {
  try {
    const response = await fetch("https://api.myshop.com/auth/register", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        name: userData.name,
        email: userData.email,
        password: userData.password,
        phone: userData.phone
      })
    });
    
    if (!response.ok) {
      throw new Error(`Registration failed: ${response.status}`);
    }
    
    const data = await response.json();
    return {
      success: true,
      message: "User registered successfully",
      userId: data.user._id,
      token: data.token
    };
  } catch (error) {
    return {
      success: false,
      message: error.message,
      error: "REGISTRATION_ERROR"
    };
  }
};
```

### 1.2 Login
```js
const loginUser = async (email, password) => {
  try {
    const response = await fetch("https://api.myshop.com/auth/login", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ email, password })
    });
    
    const data = await response.json();
    
    if (!response.ok) {
      throw new Error(data.message || "Invalid credentials");
    }
    
    localStorage.setItem("token", data.token);
    return { success: true, user: data.user, token: data.token };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 1.3 Forgot Password
```js
const forgotPassword = async (email) => {
  try {
    const res = await fetch("https://api.myshop.com/auth/forgot-password", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ email })
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error(data.message);
    
    return { success: true, message: "Reset link sent to email" };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 1.4 Reset Password
```js
const resetPassword = async (token, newPassword) => {
  try {
    const res = await fetch("https://api.myshop.com/auth/reset-password", {
      method: "PUT",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${token}`
      },
      body: JSON.stringify({ password: newPassword })
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error(data.message);
    
    return { success: true, message: "Password updated successfully" };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 1.5 Get Profile
```js
const getProfile = async () => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/user/profile", {
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Failed to fetch profile");
    
    return { success: true, profile: data };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 1.6 Update Profile
```js
const updateProfile = async (updates) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/user/profile", {
      method: "PATCH",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${token}`
      },
      body: JSON.stringify(updates)
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Update failed");
    
    return { success: true, profile: data };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 1.7 Change Password
```js
const changePassword = async (oldPass, newPass) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/user/change-password", {
      method: "PUT",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${token}`
      },
      body: JSON.stringify({ oldPassword: oldPass, newPassword: newPass })
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error(data.message);
    
    return { success: true, message: "Password changed" };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 1.8 Upload Avatar
```js
const uploadAvatar = async (file) => {
  try {
    const token = localStorage.getItem("token");
    const formData = new FormData();
    formData.append("avatar", file);
    
    const res = await fetch("https://api.myshop.com/user/avatar", {
      method: "POST",
      headers: { "Authorization": `Bearer ${token}` },
      body: formData
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Upload failed");
    
    return { success: true, avatarUrl: data.url };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 1.9 Logout
```js
const logoutUser = async () => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/auth/logout", {
      method: "POST",
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    localStorage.removeItem("token");
    return { success: true, message: "Logged out" };
  } catch (error) {
    localStorage.removeItem("token");
    return { success: false, message: error.message };
  }
};
```

### 1.10 Delete Account
```js
const deleteAccount = async (password) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/user/delete", {
      method: "DELETE",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${token}`
      },
      body: JSON.stringify({ password })
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error(data.message);
    
    return { success: true, message: "Account deleted" };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

---

## 🛍️ PART 2: PRODUCT ECOSYSTEM (10 APIs)

### 2.1 Get All Products
```js
const getAllProducts = async (page = 1, limit = 10) => {
  try {
    const res = await fetch(`https://api.myshop.com/products?page=${page}&limit=${limit}`);
    const data = await res.json();
    
    if (!res.ok) throw new Error("Failed to fetch products");
    
    return {
      success: true,
      products: data.products,
      total: data.total,
      currentPage: page,
      totalPages: Math.ceil(data.total / limit)
    };
  } catch (error) {
    return { success: false, message: error.message, products: [] };
  }
};
```

### 2.2 Get Single Product
```js
const getProductById = async (id) => {
  try {
    const res = await fetch(`https://api.myshop.com/products/${id}`);
    const data = await res.json();
    
    if (!res.ok) throw new Error("Product not found");
    
    return { success: true, product: data };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 2.3 Search Products
```js
const searchProducts = async (query, filters = {}) => {
  try {
    const params = new URLSearchParams({
      q: query,
      ...filters
    });
    
    const res = await fetch(`https://api.myshop.com/products/search?${params}`);
    const data = await res.json();
    
    if (!res.ok) throw new Error("Search failed");
    
    return { success: true, results: data.products, total: data.total };
  } catch (error) {
    return { success: false, message: error.message, results: [] };
  }
};
```

### 2.4 Get Products by Category
```js
const getProductsByCategory = async (category, sort = "newest") => {
  try {
    const res = await fetch(`https://api.myshop.com/products/category/${category}?sort=${sort}`);
    const data = await res.json();
    
    if (!res.ok) throw new Error("Category not found");
    
    return { success: true, products: data.products, category: data.category };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 2.5 Create Product (Seller)
```js
const createProduct = async (productData) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/products", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${token}`
      },
      body: JSON.stringify(productData)
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error(data.message);
    
    return { success: true, product: data, message: "Product created" };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 2.6 Update Product
```js
const updateProduct = async (id, updates) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch(`https://api.myshop.com/products/${id}`, {
      method: "PUT",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${token}`
      },
      body: JSON.stringify(updates)
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Update failed");
    
    return { success: true, product: data };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 2.7 Delete Product
```js
const deleteProduct = async (id) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch(`https://api.myshop.com/products/${id}`, {
      method: "DELETE",
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    if (!res.ok) throw new Error("Delete failed");
    
    return { success: true, message: "Product deleted" };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 2.8 Add Product Review
```js
const addReview = async (productId, rating, comment) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch(`https://api.myshop.com/products/${productId}/reviews`, {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${token}`
      },
      body: JSON.stringify({ rating, comment })
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error(data.message);
    
    return { success: true, review: data };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 2.9 Get Product Reviews
```js
const getReviews = async (productId, page = 1) => {
  try {
    const res = await fetch(`https://api.myshop.com/products/${productId}/reviews?page=${page}`);
    const data = await res.json();
    
    if (!res.ok) throw new Error("Failed to load reviews");
    
    return { success: true, reviews: data.reviews, averageRating: data.averageRating };
  } catch (error) {
    return { success: false, message: error.message, reviews: [] };
  }
};
```

### 2.10 Like/Unlike Product
```js
const toggleLike = async (productId) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch(`https://api.myshop.com/products/${productId}/like`, {
      method: "POST",
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Action failed");
    
    return { success: true, isLiked: data.isLiked, likesCount: data.likesCount };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

---

## 🛒 PART 3: CART & ORDER ECOSYSTEM (10 APIs)

### 3.1 Add to Cart
```js
const addToCart = async (productId, quantity = 1, size = null, color = null) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/cart/add", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${token}`
      },
      body: JSON.stringify({ productId, quantity, size, color })
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error(data.message || "Failed to add");
    
    return { success: true, cart: data.cart, message: "Added to cart" };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 3.2 Get Cart
```js
const getCart = async () => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/cart", {
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Failed to load cart");
    
    const subtotal = data.items.reduce((sum, item) => sum + item.price * item.quantity, 0);
    
    return {
      success: true,
      items: data.items,
      totalItems: data.items.length,
      subtotal,
      tax: subtotal * 0.15,
      total: subtotal * 1.15
    };
  } catch (error) {
    return { success: false, message: error.message, items: [] };
  }
};
```

### 3.3 Update Cart Item
```js
const updateCartItem = async (itemId, quantity) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch(`https://api.myshop.com/cart/update/${itemId}`, {
      method: "PATCH",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${token}`
      },
      body: JSON.stringify({ quantity })
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Update failed");
    
    return { success: true, cart: data };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 3.4 Remove from Cart
```js
const removeFromCart = async (itemId) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch(`https://api.myshop.com/cart/remove/${itemId}`, {
      method: "DELETE",
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Failed to remove");
    
    return { success: true, cart: data.cart };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 3.5 Apply Coupon
```js
const applyCoupon = async (code) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/cart/coupon", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${token}`
      },
      body: JSON.stringify({ couponCode: code })
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error(data.message || "Invalid coupon");
    
    return {
      success: true,
      discount: data.discount,
      message: `Coupon applied! You saved Rs.${data.discount}`
    };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 3.6 Place Order
```js
const placeOrder = async (shippingAddress, paymentMethod) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/orders", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${token}`
      },
      body: JSON.stringify({
        shippingAddress,
        paymentMethod
      })
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error(data.message || "Order failed");
    
    return {
      success: true,
      order: data.order,
      orderId: data.order._id,
      message: "Order placed successfully!"
    };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 3.7 Get Orders
```js
const getOrders = async (status = "all", page = 1) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch(`https://api.myshop.com/orders?status=${status}&page=${page}`, {
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Failed to fetch orders");
    
    return { success: true, orders: data.orders, total: data.total };
  } catch (error) {
    return { success: false, message: error.message, orders: [] };
  }
};
```

### 3.8 Get Order Detail
```js
const getOrderDetail = async (orderId) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch(`https://api.myshop.com/orders/${orderId}`, {
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Order not found");
    
    return { success: true, order: data };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 3.9 Cancel Order
```js
const cancelOrder = async (orderId, reason) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch(`https://api.myshop.com/orders/${orderId}/cancel`, {
      method: "PUT",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${token}`
      },
      body: JSON.stringify({ reason })
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error(data.message);
    
    return { success: true, message: "Order cancelled", refund: data.refund };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 3.10 Track Order
```js
const trackOrder = async (orderId) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch(`https://api.myshop.com/orders/${orderId}/tracking`, {
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Tracking not available");
    
    return {
      success: true,
      currentStatus: data.status,
      estimatedDelivery: data.estimatedDelivery,
      trackingHistory: data.history
    };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

---

## ❤️ PART 4: SOCIAL FEATURES (10 APIs)

### 4.1 Follow User
```js
const followUser = async (userId) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch(`https://api.myshop.com/social/follow/${userId}`, {
      method: "POST",
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Failed to follow");
    
    return { success: true, isFollowing: true, followersCount: data.followersCount };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 4.2 Unfollow User
```js
const unfollowUser = async (userId) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch(`https://api.myshop.com/social/unfollow/${userId}`, {
      method: "POST",
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Failed to unfollow");
    
    return { success: true, isFollowing: false };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 4.3 Get Followers
```js
const getFollowers = async (userId) => {
  try {
    const res = await fetch(`https://api.myshop.com/social/followers/${userId}`);
    const data = await res.json();
    
    if (!res.ok) throw new Error("Failed to load followers");
    
    return { success: true, followers: data.users, total: data.total };
  } catch (error) {
    return { success: false, message: error.message, followers: [] };
  }
};
```

### 4.4 Get Following
```js
const getFollowing = async (userId) => {
  try {
    const res = await fetch(`https://api.myshop.com/social/following/${userId}`);
    const data = await res.json();
    
    if (!res.ok) throw new Error("Failed to load following");
    
    return { success: true, following: data.users };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 4.5 Like Post
```js
const likePost = async (postId) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch(`https://api.myshop.com/posts/${postId}/like`, {
      method: "POST",
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Failed to like");
    
    return { success: true, isLiked: true, likesCount: data.likesCount };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 4.6 Comment on Post
```js
const addComment = async (postId, text) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch(`https://api.myshop.com/posts/${postId}/comments`, {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${token}`
      },
      body: JSON.stringify({ text })
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Failed to comment");
    
    return { success: true, comment: data.comment };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 4.7 Share Post
```js
const sharePost = async (postId, platform = "copy") => {
  try {
    const res = await fetch(`https://api.myshop.com/posts/${postId}/share`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ platform })
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Share failed");
    
    return { success: true, shareUrl: data.shareUrl };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 4.8 Save/Bookmark Post
```js
const bookmarkPost = async (postId) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch(`https://api.myshop.com/posts/${postId}/bookmark`, {
      method: "POST",
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Bookmark failed");
    
    return { success: true, isBookmarked: data.isBookmarked };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 4.9 Get Feed
```js
const getFeed = async (page = 1, type = "following") => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch(`https://api.myshop.com/feed?page=${page}&type=${type}`, {
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Failed to load feed");
    
    return { success: true, posts: data.posts, hasMore: data.hasMore };
  } catch (error) {
    return { success: false, message: error.message, posts: [] };
  }
};
```

### 4.10 Report Content
```js
const reportContent = async (contentId, contentType, reason) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/reports", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${token}`
      },
      body: JSON.stringify({ contentId, contentType, reason })
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Report failed");
    
    return { success: true, message: "Report submitted" };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

---

## 💳 PART 5: PAYMENTS & TRANSACTIONS (8 APIs)

### 5.1 Add Payment Method
```js
const addPaymentMethod = async (cardDetails) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/payments/methods", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${token}`
      },
      body: JSON.stringify(cardDetails)
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Invalid card details");
    
    return { success: true, card: data.maskedCard };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 5.2 Get Payment Methods
```js
const getPaymentMethods = async () => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/payments/methods", {
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Failed to load payment methods");
    
    return { success: true, methods: data.methods };
  } catch (error) {
    return { success: false, message: error.message, methods: [] };
  }
};
```

### 5.3 Process Payment
```js
const processPayment = async (orderId, methodId) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/payments/process", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${token}`
      },
      body: JSON.stringify({ orderId, paymentMethodId: methodId })
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Payment failed");
    
    return { success: true, transactionId: data.transactionId, status: "PAID" };
  } catch (error) {
    return { success: false, message: error.message, status: "FAILED" };
  }
};
```

### 5.4 Get Wallet Balance
```js
const getWalletBalance = async () => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/wallet/balance", {
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Failed to fetch balance");
    
    return { success: true, balance: data.balance, currency: "PKR" };
  } catch (error) {
    return { success: false, message: error.message, balance: 0 };
  }
};
```

### 5.5 Add Money to Wallet
```js
const addMoneyToWallet = async (amount) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/wallet/topup", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${token}`
      },
      body: JSON.stringify({ amount })
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Top-up failed");
    
    return { success: true, newBalance: data.balance, message: `Added Rs.${amount}` };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 5.6 Get Transaction History
```js
const getTransactions = async (page = 1, filter = "all") => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch(`https://api.myshop.com/transactions?page=${page}&filter=${filter}`, {
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Failed to load transactions");
    
    return { success: true, transactions: data.transactions, total: data.total };
  } catch (error) {
    return { success: false, message: error.message, transactions: [] };
  }
};
```

### 5.7 Request Refund
```js
const requestRefund = async (orderId, reason) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/refunds", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${token}`
      },
      body: JSON.stringify({ orderId, reason })
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error(data.message);
    
    return { success: true, refundId: data.refundId, status: "PENDING" };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 5.8 Check Refund Status
```js
const checkRefundStatus = async (refundId) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch(`https://api.myshop.com/refunds/${refundId}`, {
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Refund not found");
    
    return { success: true, status: data.status, amount: data.amount, date: data.processedAt };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

---

## 📦 PART 6: SHIPPING & ADDRESSES (7 APIs)

### 6.1 Add Address
```js
const addAddress = async (address) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/addresses", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${token}`
      },
      body: JSON.stringify(address)
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Failed to add address");
    
    return { success: true, address: data.address };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 6.2 Get Addresses
```js
const getAddresses = async () => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/addresses", {
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Failed to load addresses");
    
    return { success: true, addresses: data.addresses };
  } catch (error) {
    return { success: false, message: error.message, addresses: [] };
  }
};
```

### 6.3 Update Address
```js
const updateAddress = async (addressId, updates) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch(`https://api.myshop.com/addresses/${addressId}`, {
      method: "PATCH",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${token}`
      },
      body: JSON.stringify(updates)
    });
    
    if (!res.ok) throw new Error("Update failed");
    
    return { success: true, message: "Address updated" };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 6.4 Delete Address
```js
const deleteAddress = async (addressId) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch(`https://api.myshop.com/addresses/${addressId}`, {
      method: "DELETE",
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    if (!res.ok) throw new Error("Delete failed");
    
    return { success: true, message: "Address deleted" };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 6.5 Calculate Shipping
```js
const calculateShipping = async (addressId, items) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/shipping/calculate", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${token}`
      },
      body: JSON.stringify({ addressId, items })
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Calculation failed");
    
    return {
      success: true,
      standard: data.standard,
      express: data.express,
      estimatedDays: data.estimatedDays
    };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 6.6 Track Shipment
```js
const trackShipment = async (trackingNumber) => {
  try {
    const res = await fetch(`https://api.myshop.com/shipping/track/${trackingNumber}`);
    const data = await res.json();
    
    if (!res.ok) throw new Error("Tracking number invalid");
    
    return {
      success: true,
      status: data.status,
      currentLocation: data.currentLocation,
      estimatedDelivery: data.estimatedDelivery,
      timeline: data.timeline
    };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 6.7 Set Default Address
```js
const setDefaultAddress = async (addressId) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch(`https://api.myshop.com/addresses/${addressId}/default`, {
      method: "PUT",
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    if (!res.ok) throw new Error("Failed to set default");
    
    return { success: true, message: "Default address updated" };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

---

## 🔔 PART 7: NOTIFICATIONS (5 APIs)

### 7.1 Get Notifications
```js
const getNotifications = async (page = 1) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch(`https://api.myshop.com/notifications?page=${page}`, {
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Failed to load notifications");
    
    return {
      success: true,
      notifications: data.notifications,
      unreadCount: data.unreadCount
    };
  } catch (error) {
    return { success: false, message: error.message, notifications: [] };
  }
};
```

### 7.2 Mark as Read
```js
const markAsRead = async (notificationId) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch(`https://api.myshop.com/notifications/${notificationId}/read`, {
      method: "PATCH",
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    if (!res.ok) throw new Error("Failed to mark as read");
    
    return { success: true };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 7.3 Mark All as Read
```js
const markAllAsRead = async () => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/notifications/read-all", {
      method: "PATCH",
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    if (!res.ok) throw new Error("Failed");
    
    return { success: true, message: "All marked as read" };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 7.4 Update Notification Preferences
```js
const updateNotificationPrefs = async (preferences) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/notifications/preferences", {
      method: "PUT",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${token}`
      },
      body: JSON.stringify(preferences)
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Update failed");
    
    return { success: true, preferences: data };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 7.5 Delete Notification
```js
const deleteNotification = async (notificationId) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch(`https://api.myshop.com/notifications/${notificationId}`, {
      method: "DELETE",
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    if (!res.ok) throw new Error("Delete failed");
    
    return { success: true };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

---

## 🔍 PART 8: SEARCH & FILTERS (5 APIs)

### 8.1 Global Search
```js
const globalSearch = async (query, type = "all") => {
  try {
    const res = await fetch(`https://api.myshop.com/search?q=${query}&type=${type}`);
    const data = await res.json();
    
    if (!res.ok) throw new Error("Search failed");
    
    return {
      success: true,
      products: data.products || [],
      users: data.users || [],
      posts: data.posts || []
    };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 8.2 Filter Products
```js
const filterProducts = async (filters) => {
  try {
    const params = new URLSearchParams(filters).toString();
    const res = await fetch(`https://api.myshop.com/products/filter?${params}`);
    const data = await res.json();
    
    if (!res.ok) throw new Error("Filter failed");
    
    return { success: true, products: data.products, total: data.total };
  } catch (error) {
    return { success: false, message: error.message, products: [] };
  }
};
```

### 8.3 Sort Products
```js
const sortProducts = async (sortBy = "price", order = "asc") => {
  try {
    const res = await fetch(`https://api.myshop.com/products/sort?by=${sortBy}&order=${order}`);
    const data = await res.json();
    
    if (!res.ok) throw new Error("Sort failed");
    
    return { success: true, products: data.products };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 8.4 Get Suggestions
```js
const getSuggestions = async (query) => {
  try {
    const res = await fetch(`https://api.myshop.com/suggestions?q=${query}`);
    const data = await res.json();
    
    if (!res.ok) throw new Error("No suggestions");
    
    return { success: true, suggestions: data.suggestions };
  } catch (error) {
    return { success: false, message: error.message, suggestions: [] };
  }
};
```

### 8.5 Trending Searches
```js
const getTrendingSearches = async () => {
  try {
    const res = await fetch("https://api.myshop.com/trending");
    const data = await res.json();
    
    if (!res.ok) throw new Error("Failed to load trending");
    
    return { success: true, trending: data.trending };
  } catch (error) {
    return { success: false, message: error.message, trending: [] };
  }
};
```

---

## 🎮 PART 9: GAMIFICATION & LOYALTY (5 APIs)

### 9.1 Get Points
```js
const getLoyaltyPoints = async () => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/loyalty/points", {
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Failed");
    
    return { success: true, points: data.points, tier: data.tier };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 9.2 Redeem Points
```js
const redeemPoints = async (points) => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/loyalty/redeem", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${token}`
      },
      body: JSON.stringify({ points })
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error(data.message);
    
    return { success: true, discount: data.discount, remainingPoints: data.remaining };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 9.3 Get Badges
```js
const getBadges = async () => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/loyalty/badges", {
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("Failed");
    
    return { success: true, badges: data.badges };
  } catch (error) {
    return { success: false, message: error.message, badges: [] };
  }
};
```

### 9.4 Daily Check-in
```js
const dailyCheckIn = async () => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/loyalty/checkin", {
      method: "POST",
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error(data.message);
    
    return { success: true, pointsEarned: data.points, streak: data.streak };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

### 9.5 Spin Wheel
```js
const spinWheel = async () => {
  try {
    const token = localStorage.getItem("token");
    const res = await fetch("https://api.myshop.com/loyalty/spin", {
      method: "POST",
      headers: { "Authorization": `Bearer ${token}` }
    });
    
    const data = await res.json();
    if (!res.ok) throw new Error("No spins left");
    
    return { success: true, prize: data.prize, type: data.type };
  } catch (error) {
    return { success: false, message: error.message };
  }
};
```

---

## 📊 GRAND TOTAL: 55 APIs ACROSS 9 MODULES

| Module | APIs | Key Features |
|--------|------|--------------|
| User Auth | 10 | Register, Login, OTP, Profile, Avatar |
| Products | 10 | CRUD, Search, Reviews, Likes |
| Cart & Orders | 10 | Cart, Coupon, Order, Tracking |
| Social | 10 | Follow, Like, Comment, Feed |
| Payments | 8 | Cards, Wallet, Transactions, Refunds |
| Shipping | 7 | Addresses, Calculate, Track |
| Notifications | 5 | CRUD, Preferences |
| Search | 5 | Global, Filters, Trending |
| Loyalty | 5 | Points, Badges, Check-in |
| **TOTAL** | **55** | **Complete E-Commerce Ecosystem** |

---

## 🎯 Ab Tumhara Mission

1. **Har API ko 2 baar likho** — ek baar copy dekh kar, ek baar bina dekhe
2. **Try/Catch pattern notice karo** — har API mein same pattern hai
3. **Error handling ke types pe dhyan do**:
   - `!response.ok` check
   - `throw new Error()`
   - `catch` mein `success: false`
4. **Token handling** — kab `Bearer ${token}` lagana hai
5. **HTTP Methods** — kab GET, POST, PATCH, PUT, DELETE

**Inshallah, iske baad API calls tumhari muscle memory ban jayegi!** 🔥💪

Batao kaunse module se shuru kar rahe ho?